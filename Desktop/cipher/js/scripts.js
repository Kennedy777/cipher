var sentence = prompt("Sentence here, no periods please.");
var upperCase1 = sentence.charAt(0);
var upperCase2 = sentence.charAt(sentence.length-1);
function capitalize(mychar) {
  let upperC = mychar.toUpperCase();
  answer += upperC;
var answer2 = answer.charAt(answer.length-1) + answer.charAt(0)
let answer = "";
capitalize(upperCase1);
capitalize(upperCase2);
alert(answer);
};
